import styled from "styled-components";
import { fs } from "../../../globalStyle";

export const Wrapper = styled.div `

`

export const Img = styled.img `
  width: 100%;
`

export const Title = styled.h1 `
  font-size: ${fs.nm};
`